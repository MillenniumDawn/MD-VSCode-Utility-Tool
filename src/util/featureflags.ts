import { getConfiguration } from "./vsccommon";

const config = getConfiguration();

export const useConditionInFocus = config.useConditionInFocus;
export const eventTreePreview = config.eventTreePreview;
export const sharedFocusIndex = config.sharedFocusIndex;
export const gfxIndex = config.gfxIndex;
export const localisationIndex = config.localisationIndex;
